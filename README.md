**Степина Алёна ИТ-2 Лабораторная №3**

---

### **Задание 1**
#### **Задача 4**
**Текст задачи**  
Дроби. 
Измените сущность Дробь из задачи 1.5.5. Реализуйте следующие требования:
• Дробь не может быть изменена после создания
• Необходимо корректно обрабатывать отрицательные значения. Учтите, что знаменатель 
не может быть отрицательным. 
Продемонстрируйте работоспособность решения на примерах.

**Алгоритм решения**  
1. **Создание дроби**
   - Принимает числитель и знаменатель.
   - Проверяет: знаменатель ≠ 0, при отрицательном знаменателе знак переносится на числитель.
   - Дробь автоматически сокращается с помощью НОД (алгоритм Евклида).

2. **Арифметические операции**
   - Реализованы методы: `plus`, `minus`, `times`, `divide`.
   - Каждый возвращает **новую дробь** — объект неизменяем.
   - При делении на ноль выбрасывается исключение.

3. **Вывод**
   - `toString()` выводит дробь в формате `a/b` или целое число, если знаменатель = 1.

4. **Тестирование**
   - В `UnitOneOne.run()` реализован консольный калькулятор:
     - Пользователь вводит дроби и операции (+, −, ×, ÷).
     - Вычисления цепочкой: результат предыдущей операции — первый операнд следующей.
     - Обработка ошибок ввода через `try-catch`.


---

#### **Задача 10**
**Текст задачи**
Дороги. 
Измените сущности из задачи 1.3.3. Гарантируйте, что между двумя городами может быть только 
одна прямая дорога (другой путь может быть проложен только транзитом через другие города). 
Города можно создавать с указанием заранее заданных путей, в любой момент времени можно 
добавить новую дорогу в любой город и удалить имеющуюся дорогу.

**Алгоритм решения**  
1. **Создание города**
   - Имя должно быть непустым.
   - Дороги задаются как `Map<название, расстояние>`.
   - При создании:
     - Исключаются петли (город → сам себе),
     - Отрицательные расстояния заменяются на 1,
     - Пустые/некорректные имена пропускаются,
     - Дубликаты перезаписываются.

2. **Методы**
   - `addRoad(dest, dist)` — возвращает новый город с добавленной дорогой.
   - `removeRoad(dest)` — возвращает новый город без указанной дороги.
   - `hasRoadTo()`, `getDistanceTo()` — проверка и получение данных.
   - `getRoads()` — возвращает копию карты дорог.

3. **Неизменяемость**
   - Все изменения порождают новый экземпляр.
   - Внутренняя карта — `unmodifiable`.

4. **Тестирование**
   - В `UnitOneTwo.run()` создаётся сеть из 6 городов.
   - Тестируются:
     - Добавление/удаление дорог,
     - Защита от ошибок (A→A, отрицательные числа),
     - Перезапись существующих дорог,
     - Проверка связей.

---

### **Задание 2**
#### **Задача 5**
**Текст задачи**  
Маршрут.
Данная задача предполагает разработку новой сущности на основе той, что была получена в 
задаче 1.4.8 (Город). Сущность Город может быть доработана по своему усмотрению для более 
удобного использования.
Основная идея задачи в разработке такой сущности, которая будет представлять собой маршрут 
между двумя городами. Данный маршрут в любой момент времени можно получить как массив 
для дальнейшего использования. Сущность Маршрут имеет следующие характеристики:
• Имеет Город начала и Город конца пути. 
• Инициализация Маршрута может быть выполнена только если указана точка начала и 
конца пути. Если указано nullзначение - то ошибка.
• Точку начала и конца можно изменить в любой момент времени, но они всегда должны 
существовать.
• Может вернуть массив Городов, представляющий собой маршрут из начала в конец. 
Массив содержит все Города (в порядке очереди) через которые надо пройти что бы 
попасть из Города начала в Город конца, причем и начало и конец также содержатся в 
этом массиве. Алгоритм формирования пути в данном случае не существенен, можно 
выбрать вариант со случайным путем, путем проходящим через наименьшее число 
городов, или самым дешевым путем. Если путь найти невозможно – возвращается пустой 
массив. 
• Маршрут может быть приведен к строке, которая будет возвращать название всех 
городов маршрута в порядке очередности.
• Создание объекта и изменение точек начала и конца выполняется за константное время 
O(1).
Воспользуйтесь картой городов из задачи 1.3.3 и выведите маршрут из Города F в Город D

**Алгоритм решения**  

1. **Создание городов**
- Создаются объекты класса `City` с уникальными именами (например: A, B, C, D, E, F).
- Каждый город содержит неизменяемое множество связанных с ним городов (`Set<City>`).

2. **Построение дорожной сети**
- С помощью метода `addRoad(City)` добавляются односторонние дороги.
- Для двунаправленных связей дороги добавляются в обе стороны.

3. **Формирование графа**
- Все города и их связи объединяются в общий граф:
- Граф передаётся в `Route` как основа для поиска.

4. **Поиск маршрута**
- Используется класс `Route` с указанием начального и конечного города.
- Алгоритм поиска:
  1. Применяется **поиск в ширину (BFS)** — гарантирует нахождение кратчайшего пути по количеству рёбер.
  2. Ведётся отслеживание пройденных городов (`visited`) и предшественников (`predecessors`).
  3. При достижении целевого города путь восстанавливается в обратном порядке.

5. **Вывод результата**
- Маршрут выводится в виде строки: `F → B → C → D`.
- Также возвращается массив городов (`City[]`).
- Если путь не найден — возвращается пустой результат.



---

### **Задание 3**
#### **Задача 1**
**Текст задачи**  
Запретная Дробь. 
Измените сущность Дробь, полученную в задаче 2.1.4. Гарантируйте, что невозможно создать 
такой подвид дроби, который позволял бы создавать Дроби с изменяемым состоянием.

**Алгоритм решения**  
1. **Приветствие** и инструкция.
2. **Ввод первой дроби** через `createFraction(scanner)`.
3. **Цикл ввода операций**:
   - Пользователь вводит знак: `+`, `-`, `*`, `/`, или `=`.
   - При `=` — вывод результата и завершение.
   - При неверном знаке — сообщение об ошибке, повтор ввода.
4. **Ввод второй дроби**.
5. **Выполнение операции**:
   - Вызывается соответствующий метод `Fraction`.
   - Результат выводится в формате: `a/b + c/d = e/f`.
6. **Обновление текущего значения** — результат становится новым `current`.
7. **Обработка ошибок**:
   - Некорректный ввод числа — предложение ввести снова.
   - Исключения перехватываются, программа не падает.

##### 🔹 Метод `createFraction()`
- Запрашивает числитель и знаменатель.
- Обрабатывает:
  - Нецелые значения (`NumberFormatException`),
  - Нулевой знаменатель (`IllegalArgumentException`).
- Повторяет ввод до получения корректной дроби.


---

### **Задание 4**
#### **Задача 2**
**Текст задачи**
Дробь это число. 
Измените сущность Дробь, полученную в задаче 2.3.1. Дробь должна быть подтипом класса 
Number. Данный класс входит в стандартную редакцию языка Java.

**Алгоритм решения**  

Улучшенная версия предыдущего калькулятора с расширенным выводом.

1. **Приветствие** и инструкция.
2. **Ввод первой дроби** через `createFraction()`.
3. **Цикл операций**:
   - Пользователь вводит операцию: `+`, `-`, `*`, `/`, или `=`.
   - При `=` — вывод финального результата во всех форматах и завершение.
   - При неверной операции — сообщение об ошибке, повтор ввода.
4. **Ввод второй дроби**.
5. **Выполнение операции** через `performOperation()` (использует `switch`).
6. **Вывод промежуточного результата**:
   - Арифметическое выражение,
   - Результат как дробь,
   - Все числовые представления (`int`, `long`, `float`, `double`).
7. **Обновление текущего значения**.
8. **Обработка исключений** — программа продолжает работу при ошибках ввода.




---

### **Задание 5**
#### **Задача 1**
**Текст задачи**  
Сложение. 
Разработайте метод, который принимает набор числовых значенийи возвращает их сумму в 
вещественной форме. 

**Алгоритм решения**  

1. **Приветствие** и инструкция:
   - Ввод чисел по одному,
   - Пустая строка — завершение ввода.
2. **Цикл ввода**:
   - Пользователь вводит значение.
   - При пустом вводе — выход из цикла.
3. **Парсинг строки** через `parseNumber(input)`:
   - **Обыкновенная дробь** (формат `a/b`):
     - Распознаётся с помощью регулярного выражения: `(-?\\d+)\\s*/\\s*(\\d+)`.
     - Проверяется, что знаменатель ≠ 0.
     - Преобразуется в `double`: `(numer / denom)`.
   - **Десятичное число** (с точкой или экспонентой) → `Double.parseDouble()`.
   - **Целое число** → `Integer.parseInt()`.
   - Если не распознано — выводится ошибка, значение игнорируется.
4. **Добавление в список** `List<Number>` — благодаря иерархии `Number`, все типы хранятся единообразно.
5. **Вычисление суммы**:
   - Вызывается `sumNumbers()` (аналогичный `MathUtils.sum()`).
6. **Вывод результата**:
   - Количество введённых чисел,
   - Итоговая сумма (с точностью до 6 знаков).


---

### **Задание 6**
#### **Задача 5**
**Текст задачи**  
Сравнение городов.
Измените сущность Город, полученную в задаче 2.1.10. Переопределите метод сравнения 
объектов по состоянию таким образом, чтобы два Города считались одинаковыми тогда, когда у 
них одинаковый набор путей в другие города. 

**Алгоритм решения**  

1. **Создание дроби (`Fraction`)**  
   - Принимает числитель и знаменатель.  
   - Проверяет: знаменатель ≠ 0.  
   - Если знаменатель < 0 — переносит знак на числитель.  
   - Автоматически сокращает дробь с помощью НОД.

2. **Нормализация**  
   - Все дроби хранятся в сокращённом виде (например, `2/4` → `1/2`).

3. **Сравнение (`equals`)**  
   - Две дроби равны, если их числители и знаменатели совпадают после нормализации.  
   - Пример: `1/2.equals(2/4)` → `true`, потому что обе становятся `1/2`.

4. **Ввод дробей (`UnitSix`)**  
   - Пользователь вводит две дроби по числителю и знаменателю.  
   - Ошибки (не число, деление на ноль) обрабатываются — ввод повторяется.

5. **Сравнение и вывод**  
   - Программа показывает обе дроби и сообщает, равны ли они.  
   - Использует `equals()` для проверки.


---

### **Задание 7**
#### **Задачи 1, 2, 3**
**Текст задач**  
1. Навести порядок. 
Данная задача предполагает реорганизацию ранее написанных классов. Расположите все ранее 
написанные классы по пакетам таким образом, чтобы логически близкие классы оказались 
сгруппированы друг с другом. Имена пакетов должны иметь как минимум трехсоставную форму, 
вида: ru.surname.type. Вместо surname следует подставить свою фамилию, а вместо type
подставить название логического блока. Например, классы описывающие точку, линию, ломаную 
линию, фигуру, квадрат, треугольник, круг и прямоугольник можно расположить в пакете 
ru.surname.geometry

2. Главный метод.
Создайте пакет ru.surname.main (вместо surname необходимо подставить собственную 
фамилию) в котором расположить класс с точкой входа в исполнение программы (public static void
main). Также следует проверить, что ни в одном другом пакете нет классов имеющих точку входа 
в исполнение программы.

3. Возведение в степень. 
Создайте метод принимающий две строки, в которых будут записаны числа X и Y. Возвращает 
метод результат возведения X в степень Y. Для преобразования строки в число следует 
использовать метод Integer.parseInt, а для возведения в степень метод Math.pow. Вызовите 
разработанный метод передав туда параметры командной строки полученные точкой входа в 
программу. Реализуйте метод так, что бы для возведения в степень и преобразования строки 
использовались короткие имена статических методов.

**Алгоритм решения**  

#### 1. **Старт программы**
- Выводится заголовок: *"Возведение в степень"*.
- Пояснение: пользователь должен ввести два числа:
  - `X` — основание,
  - `Y` — показатель степени.


#### 2. **Ввод данных**
Используется метод `readInput(Scanner, String)` для безопасного ввода:

- **Построчный ввод** через `scanner.nextLine()`.
- **Проверка на пустой ввод**:
  - Если строка пустая или содержит только пробелы — запрос повторяется.
  - Пока пользователь не введёт непустую строку.
- Возвращает введённую строку (без обрезки пробелов — обрезка делается позже).


#### 3. **Обработка и преобразование чисел**
Выполняется в методе `power(String xStr, String yStr)`:

1. **Очистка строк**: `trim()` удаляет лишние пробелы.
2. **Преобразование в целые числа** через `parseInt()` (внутренне — `Integer.parseInt()`).
   - Если строка не является целым числом → выбрасывается `NumberFormatException`.
3. Полученные значения `x` и `y` передаются в функцию возведения в степень.


#### 4. **Возведение в степень**
- Реализовано через стандартный метод `Math.pow(x, y)`, результат приводится к `double`.
- Поддерживает:
  - Положительные степени: \( 2^3 = 8 \),
  - Отрицательные степени: \( 2^{-3} = 0.125 \),
  - Нулевую степень: \( x^0 = 1 \),
  - Основание 0: \( 0^n = 0 \) при \( n > 0 \), но \( 0^0 \) — не определено (в Java даёт 1.0).

Результат возвращается как `double`, чтобы корректно обрабатывать дробные значения при отрицательных степенях.


#### 5. **Вывод результата**
- Форматированный вывод с помощью `System.out.printf`:
  ```java
  Результат: 2 ^ 3 = 8
  ```
- При отрицательной степени результат может быть дробным, но округляется до целого (`%.0f`) — так как по условию ожидаются целые числа.


#### 6. **Обработка ошибок**
- **`NumberFormatException`**:
  - Возникает, если ввод не является целым числом (например, "abc", "3.14").
  - Перехватывается, выводится понятное сообщение.
- **Другие исключения**:
  - Обрабатываются общим блоком: `"Ошибка при вычислении: ..."`
- Программа не падает — завершается корректно после ошибки.


---

### **Задание 8**
#### **Задача 4**
**Текст задачи**  
Клонирование точки.
Измените сущность Точка из 2.6.2. Переопределите метод клонирования, унаследованный от 
класса Object, таким образом, чтобы при его вызове возвращался новый объект Точки, значения 
полей которого будут копиями оригинальной Точки.

**Алгоритм решения**  

#### 1. **Класс `Point` — точка на плоскости**
Представляет точку с координатами `(x, y)` в двумерном пространстве.

##### 🔹 Конструктор и поля
- Поля:
  - `private double x`
  - `private double y`
- Конструктор:
  ```java
  public Point(double x, double y)
  ```
  Инициализирует координаты точки.

##### 🔹 Методы доступа
- `getX()` — возвращает значение `x`
- `getY()` — возвращает значение `y`

##### 🔹 Форматированный вывод (`toString`)
- Переопределён для удобного отображения координат
- Пример: `{1.50; 2.70}` — значения округляются до двух знаков после запятой.

##### 🔹 Клонирование (`clone`)
- Класс реализует интерфейс `Cloneable`.
- Переопределён метод `clone()`:
  


#### 2. **Тестирование клонирования (`UnitOct.run`)**

1. **Создание оригинальных точек**
 
2. **Вывод оригиналов**
   - Вызывается `System.out.println(p1);` → автоматически вызывает `toString()`.

3. **Клонирование каждой точки**
   - Для каждой точки вызывается `clone()` → создаётся новый объект с теми же координатами.

4. **Вывод склонированных точек**
   - Аналогично оригиналам.
   - Проверяется, что формат и значения совпадают.

